<!DOCTYPE html>
<html>

    <head>
        <title>Capstone</title>
        <%= csrf_meta_tags %>
        <%= csp_meta_tag %>
    </head>

    <div class="row">
        <div class="column side"></div>
        <div class="column middle">
            <div class="flex-container">
                <div class="container-text">
                    Here is some sample text. This page is for the consent as well
                    as an overview of the project. In the future, this will be
                    filled but for test and purposes, this is currently filler text.
                    Please enter your given alphanumeric code and click the button to login.
                </div>
            </div>

            <div class="flex-container">
                <form action="users#create" method="POST" autocomplete="off">
                    <div class="form-actions">
                        <input type="text" name="user_id" class="form-control">
                    </div>
                    <div class="form-actions">
                        <button type="submit" class="btn btn-default">Login</button><br>
                    </div>
                </form>
            </div>

            <div class="flex-container">
                <div class="flex-container-vertical">
                    <% flash.each do |key, value| %>
                        <div class="alert alert-<%= key %>"><%= value %></div>
                    <% end %>
                </div>
            </div>
        </div>
        <div class="column side"></div>
    </div>

</html>
