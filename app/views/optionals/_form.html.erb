<%= form_with(model: optional, local: true) do |form| %>

  <div class="field">
    <%= form.text_area :response %>
  </div>

  <div class="actions">
    <%= form.submit "Submit" %>
  </div>
<% end %>
