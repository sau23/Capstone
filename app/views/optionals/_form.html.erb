<%= form_with(model: optional, local: true) do |form| %>

  <div class="field">
    <%= form.text_area :response, maxlength: 1000 %>
  </div>

  <div class="actions">
    <%= form.submit "Submit" %>
  </div>
<% end %>
